{
  "name": "checkbox",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "rimraf dist .parcel-cache && concurrently \"npm run watch:sass\" \"npm run start:parcel\"",
    "start:parcel": "parcel index.html",
    "watch:sass": "sass --watch style.scss style.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "concurrently": "^8.2.2",
    "parcel": "^2.10.3"
  },
  "dependencies": {
    "rimraf": "^5.0.5"
  }
}
